# Secrets — never commit
.env

# Jupyter Book build outputs
_build/
.jupyter_cache/

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.venv/
*.egg-info/

# Archive — legacy notebooks with hardcoded API keys
archive/

# JustETF raw scrape outputs — do NOT commit scraped third-party data to a public repo
data/raw/

# Output files are kept so the book can build without re-running the pipeline

# IDE
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db
